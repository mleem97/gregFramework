# FFM.Langserver.Compat Template

Drop adapter manifests into:

- `Data Center/Mods/FFM.Langserver.Compat`

Use `adapter-template.json` as baseline for language runtimes (Rust, Python, Lua, Delphi, etc.).
