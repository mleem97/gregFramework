# Ralph Loop Prompt (non-agent)

Use `python script.py` to initialize Ralph env.
Use `python script.py status` to inspect queue state.
If a `.lock` file is stale (>10 minutes), rename it back to `.todo`.
